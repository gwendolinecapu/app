/**
 * GitHub Integration - DISABLED
 * 
 * This integration is disabled because @octokit/rest is not installed.
 * 
 * To enable:
 * 1. cd functions && npm install @octokit/rest
 * 2. Rename this file to github.ts
 * 3. Set config: firebase functions:config:set github.token="..." github.owner="..." github.repo="..."
 */

// Export nothing - integration is disabled
export { };
